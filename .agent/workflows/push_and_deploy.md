---
description: Push changes to remote and deploy to GitHub Pages
---
1. Check commit author
   - `git log -n 5`
   - Ensure specific user commits are correct (only `yasabihhagure`)
2. Push changes to remote
   - `git push`
2. Deploy to GitHub Pages
   - `npm run deploy`
